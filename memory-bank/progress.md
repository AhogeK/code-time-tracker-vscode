# Progress

> 里程碑进度。条目从「进行中」移入「已完成」时保留原描述（AGENTS.md R2）。

## Milestone Overview

| 里程碑 | 状态 | 目标版本 |
|---|---|---|
| 仓库初始化（GitHub + 分支策略） | ✅ 完成 | 0.1.0 |
| AI 协作架构（AGENTS.md + memory-bank + SKILL_GRAPH） | ✅ 完成 | 0.1.0 |
| 非 AI 项目内容（README/CONTRIBUTING/CoC/SECURITY/LICENSE） | ✅ 完成 | 0.1.0 |
| **知识库体系升级（渐进式披露 + 回源 + 漂移检测）** | ✅ 完成 | 0.1.0 |
| 本地追踪核心（活动监听 + 空闲检测 + 会话切分） | ⏳ 未开始 | — |
| SQLite 对接（与插件端同一个库） | ⏳ 未开始 | — |
| 统计与状态栏 | ⏳ 未开始 | — |
| 云同步（ctt-server 对接） | ⏳ 未开始 | — |

## 已完成

### 语言词表 v2（2026-09-17，第七轮）

后端告知 v1 → v2（canonical 92 → 842、aliases 75 → 489），要求两端同步。
按 R31 回源核对（`shasum -a 256` + 解析 JSON），并**纠正了一处此前的定性错误**。

**核实**（全部吻合）：sha256 `be7de602…a8ea04` ✅ ｜ 39845 字节 ✅ ｜
canonical **842** / aliases **489** / nonLanguages **76** ✅ ｜
抽查 Astro、Elixir、Erlang、Haskell、OCaml、Scala、Solidity、Svelte、Nix、Zig、Nim、
Fortran、COBOL、Pascal、Ada **全部收录** ✅ ｜ 插件端**已是 v2**，同步只差本仓库。

**纠正**：词表**不是**「待服务端实现的 HTTP 接口」，而是**各客户端内置的逐字节副本**
（先例：插件端 `src/main/resources/language/vocabulary.json`）。此前记成「快照待对接」
是错的，已改为文件副本的约定与规格（`server-api/references.md`「语言词表」）。

**用途经证据确认**：插件端 `LanguageVocabulary.normalize()` **只在 `StatsRepository:551`
被调用**——本地统计查询路径，**不在上传路径**。印证「上传原样、展示时归一」，
也说明词表唯一用途是**本地合桶**。

**v1 → v2 修的是方向性错误**：v1 从「本机能枚举到的 IDE fileType」反推，
把某台机器装了什么当成语言全集 —— 750 种真实语言在服务端根本不存在，
客户端送这些值得到「未知语言」，**读起来像服务端不认识，真相是词表里从来没有它**。
v2 改为**词表 = 标准本身**（Linguist 全集 + 7 个本地扩展）。

**接入**（用户选定「内联进 bundle」）：

| 项 | 内容 |
|---|---|
| ① 复制 | `src/language/vocabulary.json` —— `cmp` 与服务端**逐字节一致** |
| ② 核对 | `shasum -a 256` = `be7de602…a8ea04` ✅；39845 字节 ✅ |
| ③ 测试 | 新增 `src/test/vocabulary.test.ts`（4 例）：sha256 + 大小、v2 计数 842/489/76、v2 新增的 15 种语言、分类合法性 —— **全绿** |
| 构建 | `tsconfig.json` 开 `resolveJsonModule`；`.vscodeignore` 已排除 `src/**` 与 `out/**` |
| 版本 | `0.1.0 → 0.1.1`（PATCH：新增数据资产与测试，无用户可见功能） |

**一个构建细节**：「逐字节一致」指**仓库里的源文件**，不是构建产物 —— `tsc` 会重新序列化
（39845 → 45488 字节），esbuild 内联时同样重新生成。故哈希测试钉源文件。

**尚未生效的部分**：`dist/extension.js` **还没有**词表 —— 无代码 import 它。
esbuild 的 JSON loader 是内置的，本地统计层一旦 import 即自动内联，**不需要改 esbuild 配置**。

**状态**：✅ 三件事全部完成（复制 / 核对 / 测试），构建接入已就位。**待授权提交**。

### 术语碰撞与「回填」更正（2026-09-16，第六轮）

**触发**：用户指出 `server-api/scenarios.md` 的「不要归一化」与同领域 P10 的「归一化」是两个
互不相干的含义，而该行恰好含「语言」，会被读成「语言不要归一化」——与 P10 直接冲突。

**全库排查**（42 处「归一」）：用**时长语义**的只有 2 处，其中 `meta.md` 已消歧、
`scenarios.md` 那处有歧义。**但排查还发现第二处同类碰撞**：`sync-client/principles.md`
P10 的表格列头写作「谁做归一化」，却同时覆盖语言、时长、时区、schema 四行。

| 位置 | 处理 |
|---|---|
| `server-api/scenarios.md` | 改为「**不要按 merge 口径合并时长**」，并注明本行说的是时长语义、与 P10 无关 |
| `server-api/meta.md` | **把「归一化」这个词本身登记进消歧表**：两个含义并列，标注「本领域禁止裸用」 |
| `sync-client/principles.md` | 表格列头改为中性的「谁负责转化」；P10 末尾加术语提醒并指向消歧表 |

**顺手更正一处事实偏差**：此前多处写「归一化并**回填历史数据**」，而源码显示是
**读取时归一化**（`StatsCalculator` 查询期），列里始终存原样值——历史数据不是被改写，
而是每次查询时被重新解释。已在 5 个文件统一更正，`progress.md` 保留更正溯源。

**教训**：术语碰撞不会只出现在一处。**修一处必须全库搜同一个词**——这次搜出第二处
（P10 表格列头），否则下次仍会被误读。

**状态**：✅ 完成。**待授权提交**。

### 第一至五轮（2026-09-14 → 2026-09-16，已归档）

仓库初始化 → AI 协作架构（R1–R31）→ 初期完善（移除 `.opencode/`、重建 `SKILL_GRAPH.md`、
补齐非 AI 文档）→ 知识库体系升级（渐进式披露、事实回源、元数据契约、漂移检测）→
接收后端方向并更正语言/schema 处理 → 语言归一化源码核实。

> 完整记录见 [`archives/2026-09-17-progress-rounds-1-5.md`](archives/2026-09-17-progress-rounds-1-5.md)

## 进行中

（无）

## Backlog

- `docs/` 面向用户文档（扩展使用手册、同步故障排查）—— 功能落地后再写

## 未决（需用户决策）

见 `techContext.md`「已决」末段的待细化项（SQLite 接入方式、空闲检测阈值）——
均属架构级变更，AGENTS.md R8 要求先确认再实施。

另有一项**等待外部输入**：语言词表快照的对接文档（后端在服务端实现落地后提供）。
收到前不得自行设计格式。
